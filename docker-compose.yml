version: '3.8'

services:
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME= # Boş bırakın
      - MONGO_INITDB_ROOT_PASSWORD= # Boş bırakın

volumes:
  mongo_data: